if [ $# -eq 1 ]; then
    clear && iverilog -Wall -g2012 -Y .sv -y rtl tb/wave_$1.v
else
    rtlargs=()
    for arg in "$@"; do
        rtlargs+=("rtl/${arg}.sv")
    done

    clear && iverilog -Wall -g2012 "${rtlargs[@]}" tb/wave_$1.v && /workspace/a.out
fi