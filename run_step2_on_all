for file in rtl/*.sv; do
    filename="${file%.*}"    # 1. Strip the "rtl/" prefix from the front
    no_prefix="${file#rtl/}"
    
    # 2. Strip the extension from the back
    clean_name="${no_prefix%.*}"
    ./step2 "$clean_name"
done
